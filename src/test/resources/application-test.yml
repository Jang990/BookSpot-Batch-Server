opensearch:
  serverUrl: http://127.0.0.1:9200
  username: admin
  password: admin

backend.url: http://localhost:8080
spring:
  sql.init.mode: always # schema.sql data.sql 실행
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/BookSpot_Test?rewriteBatchedStatements=true&allowLoadLocalInfile=true
    hikari:
      username: root
      password: 1234

  jpa:
    hibernate:
      ddl-auto: none